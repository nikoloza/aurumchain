node_modules/
.parcel-cache/
dist/
.cache/
*.log
.env
.env.local
.env.*.local
.DS_Store
.symbols_cache/
.symbols_local/
.vscode/
.npm
dist-brender/
.frank_tmp/

# Reference worktree: the pre-Symbols source, checked out on `main`.
# `git worktree add .legacy main` — never committed on this branch.
.legacy/

# Local media/design assets — never committed.
design.mp4
*.mp4

# Generated Note copies of docs/ — rebuild with `bun run docs:sync`
packages/landing/docs/
