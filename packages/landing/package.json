{
  "name": "@fractyco/landing",
  "version": "1.0.0",
  "description": "Fractyco \u2014 public marketing site",
  "author": "fractyco",
  "private": true,
  "scripts": {
    "start": "portless fractyco sh -c 'rm -rf .parcel-cache && smbls start --no-cache -p $PORT'",
    "build": "rm -rf .parcel-cache && smbls build",
    "start:port": "rm -rf .parcel-cache && smbls start --no-cache"
  },
  "dependencies": {
    "@fractyco/brand": "workspace:*"
  }
}
